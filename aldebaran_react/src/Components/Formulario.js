import React from 'react';

const Formulario = () => {
    return (
        <input
            type="password"
            id="inputPassword"
            class="form-control mt-5"
            placeholder="Password"
            required
        />
    );
}

export default Formulario;